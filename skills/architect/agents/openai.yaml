# OpenAI Codex adapter. This file supplies the interface metadata Codex shows in
# its agent picker. The skill's actual instructions live in ../SKILL.md, which the
# Agent Skills client installs alongside this file and loads when the skill runs.
interface:
  display_name: "Architect"
  short_description: "Settle load-bearing web decisions"
  default_prompt: "Run the architect skill: read its SKILL.md, settle the load-bearing decision, and write it down as a build spec with acceptance criteria before any implementation starts."
