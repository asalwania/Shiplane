# outline mode: add

A scope file already exists for this project, and one new named feature needs enrolling — not a full replan.

1. Read the existing scope file; don't re-run the greenfield or brownfield interview.
2. Ask only what this one feature needs that isn't already implied by the existing flows and support target.
3. Insert it into the build order at the right dependency position — after whatever it depends on, not just appended at the bottom regardless of order.
4. If it needs a delivery approach different from the project default, record the override on this item only.
5. Leave everything else in the file untouched.

## Handoff

Point to `blueprint` if the new item has an open load-bearing decision, or straight to `build` if it doesn't.
