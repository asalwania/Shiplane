# outline mode: plan-brownfield

Code already exists, but no `docs/scope/` file yet for this project.

1. If `AGENTS.md` doesn't exist either, point to `survey` first — plan against a real understanding of the stack, not a guess.
2. Read the routes/pages that already exist and enroll them as `done` before planning new work, so the file reflects reality on day one rather than starting the scope empty against a half-built app.
3. Ask what's next: the flows the developer actually wants built, same as the greenfield interview but scoped to what's missing.
4. Ask which delivery approach fits (see the four files under `../approaches/`) and record it as the project default.
5. Pick the workflow depth from the table in `SKILL.md`.
6. Write `docs/scope/<project>.md`, with existing work already marked `done` and new work ordered below it.

## Handoff

Point to `blueprint` for the first new item that has an open load-bearing decision, or straight to `build` if the decision already exists.
