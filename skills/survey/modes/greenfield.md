# survey mode: greenfield

Right after `blueprint` (in `architecture` mode) chose the stack and the project was scaffolded with that stack's own setup tool. There's a real, if small, project to read, not an empty folder.

1. Read `docs/specs/000-stack.md` (or wherever `blueprint` wrote the stack decision) first, so `AGENTS.md` records the decision's outcome, not a repeat of deriving it.
2. Read the scaffolded project the same way [`../SKILL.md`](../SKILL.md)'s "What it inspects" section describes; confirm the setup tool actually produced what the spec chose, rather than assuming it did.
3. Write `AGENTS.md` fresh; there's nothing to reconcile against yet.

## Handoff

Point to `outline` if the scope file wasn't already written before the stack decision, otherwise straight to `blueprint` for the first decision at the feature level.
